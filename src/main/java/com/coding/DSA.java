package com.coding;

import common.*;
import dsa.datastructure.linear.*;
import dsa.datastructure.nonlinear.TreeClass;

public class DSA {
    public static void run() {
        /* Trial */


        /* LINEAR */
        Search search = new Search();
//        Logs.print("Binary Search: " + search.binarySearch(new int[]{2, 5, 6, 8, 9, 10}, 0, 5, 6));
//        Logs.print("Linear 2D Search: " + search.linerSearch2D(new String[][]{{"hay", "hay"}, {"hay", "hay"},
//                {"hay", "hay"}, {"hay", "hay"}, {"needle", "hay"}, {"hay", "hay"}}, "needle"));
//        Logs.print("Linear Search: " + search.linearSearch(new int[]{2, 3, 4, 10, 40}, 10));
//        Logs.print("TreeNode Search: " + search.searchInTree(TreeNode.getIntTree(new int[]{2, 3, 4, 10, 40},0), 10));
//        Logs.print("Binary Search TreeNode (BST) Search: " + search.searchInBST(TreeNode.getBSTTree(new int[]{2, 3, 4, 10, 40}), 40));


        /* NON-LINEAR */
        TreeClass treeClass = new TreeClass();
//        TreeNode root = TreeNode.getTree(new Object[]{1, 2, 30, 40, 5, 6}, 0);
//        TreeNode subRoot = TreeNode.getTree(new Object[]{30, 40, 700}, 0);
//        TreeNode randomNode = treeClass.treeNode(root, 6);
//        TreeNode rootBST = TreeNode.getBSTTree(new int[]{40, 3, 1, 10, 2});
//        treeClass.inOrderTraversal(rootBST);
//
//        Logs.print("Preorder Traversal");
//        treeClass.preOrderTraversal(root);
//        Logs.lineBreak(1);
//        Logs.print("Inorder Traversal");
//        treeClass.inOrderTraversal(root);
//        Logs.lineBreak(1);
//        Logs.print("Postorder Traversal");
//        treeClass.postOrderTraversal(root);
//        Logs.lineBreak(1);
//        Logs.print("Level order Traversal");
//        treeClass.levelOrderTraversal(root);
//        Logs.lineBreak(1);
//
//        Logs.print("Get the node address from a tree: " + treeClass.treeNode(root, 5));
//        Logs.print("Get total count of a tree: " + treeClass.totalNodeCount(root));
//        Logs.print("Max height/depth of a tree: " + treeClass.maxDepthOrHeight(root));
//        Logs.print("Depth of a current node from a tree: " + treeClass.depth(root, randomNode, 0));
//        Logs.print("Level of a current node from a tree: " + treeClass.level(root, randomNode));
//        Logs.print("Height of a current node from a tree: " + treeClass.height(root.right));
//        Logs.print("Sum of a current node from a tree: " + treeClass.sum(root));
//        Logs.print("Find parent in a tree: " + treeClass.findParent(root, randomNode));
//        Logs.print("Is these two nodes are cousin in a tree: " + treeClass.isCousin(root, root.left.left, root.right.left));
//        Logs.print("Is the tree divisible in 2 halves: " + treeClass.isDivisibleByTwoHalves(root));
//        Logs.print("Is the tree perfect binary tree: " + treeClass.isPerfectBinary(root));
//        Logs.print("Is the tree a subtree of another tree: " + treeClass.isSubTree(root, subRoot));


    }
}
